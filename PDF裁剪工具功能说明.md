# PDF 裁剪工具 · 功能说明

> 面向使用者（本人）的操作说明文档。文中的截图位置均已用占位符标注，请按提示截图并替换为实际图片。

## 一、功能概述

**PDF 裁剪工具**是 myToolbox 工具箱内置的一款轻量 PDF 处理工具，核心能力是**通过鼠标框选的方式，截取 PDF 页面中的指定区域**，并支持两种用途：

| 能力 | 说明 |
| ---- | ---- |
| 🖨️ 打印 | 直接打印裁剪后的内容（仅输出你框选的区域） |
| 📥 导出 PDF | 生成一个只包含已裁剪内容的新 PDF 文件 |

工具特点：
- **所见即所得**：在页面大图上直接拖拽框选，框选区域实时高亮，直观预览结果。
- **可精确微调**：框选后可拖动 8 个控点调整边界，并显示裁剪比例，支持逐页独立裁剪。
- **批量统一**：可将当前页的裁剪框一键应用到所有页面。
- **轻量安全**：全部在本机处理，PDF 不会上传到任何服务器。

---

## 二、功能入口

从工具箱首页的启动台网格中，点击 **「PDF 裁剪」** 图标（✂️）进入。

> 📷 **截图 1**：首页启动台中「PDF 裁剪」工具入口
> 截图位置：`./screenshots/01-entry.png`

---

## 三、操作流程

### 步骤 1：上传 PDF 文件

进入工具后，点击上传区域或选择本地 PDF 文件。加载成功后，页面会在主区域按页纵向展示，底部出现操作工具栏。

- 支持格式：`*.pdf`
- 加载中会显示「正在解析 PDF…」提示

> 📷 **截图 2**：上传前页面（上传区空态）
> 截图位置：`./screenshots/02-upload-empty.png`

> 📷 **截图 3**：PDF 加载完成后的页面列表与工具栏
> 截图位置：`./screenshots/03-loaded.png`

### 步骤 2：框选要保留的区域

在任意一页的预览图上，**按住鼠标左键拖动**，拖出蓝色虚线框即为选中区域。

- 框选外的区域会被半透明遮罩压暗，框内保持高亮，方便确认。
- 拖拽过程中右上角会实时显示当前框选的**比例**（例如 `62% × 40%`）。
- 松开鼠标即确认该页裁剪框，页面状态标记为「已框选」。

> 📷 **截图 4**：在某页拖拽框选区域（可拍「正在拖动」的过程）
> 截图位置：`./screenshots/04-dragging.png`

> 📷 **截图 5**：框选完成后的效果（框高亮 + 页面显示「已框选」）
> 截图位置：`./screenshots/05-cropped.png`

### 步骤 3：调整 / 微调裁剪框

框选完成后，裁剪框四角与四边会出现 **8 个白色控点**：

- 拖动角点可同时调整宽高。
- 拖动边点可单独调整水平/垂直方向。
- 页面卡片右上角的 **「清除」** 按钮可移除该页的裁剪框。

> 📷 **截图 6**：裁剪框的 8 个控点效果
> 截图位置：`./screenshots/06-handles.png`

### 步骤 4：批量操作（可选）

底部工具栏提供两个便捷按钮：

| 按钮 | 作用 |
| ---- | ---- |
| **应用到所有页** | 把当前选中页的裁剪框复制到每一页 |
| **重置裁剪** | 清空所有页面已设置的裁剪框 |

工具栏会实时显示：`共 X 页 · 已裁剪 Y 页`。

> 📷 **截图 7**：底部工具栏（含「应用到所有页 / 重置裁剪 / 打印 / 导出 PDF」）
> 截图位置：`./screenshots/07-toolbar.png`

### 步骤 5：打印或导出

完成上述裁剪后，二选一：

- **🖨️ 打印**
  只打印你框选的内容。桌面版（Tauri）调用系统原生打印面板，无多余界面元素。
- **📥 导出 PDF**
  弹窗选择保存位置，生成只包含「已裁剪」内容的新 PDF 文件（文件名自动追加 `-cropped` 后缀）。

> ⚠️ 只有**至少存在一页已框选**时，打印 / 导出按钮才会点亮可用。

> 📷 **截图 8**：点击「打印」后调起的系统打印预览
> 截图位置：`./screenshots/08-print.png`

> 📷 **截图 9**：点击「导出 PDF」后的保存对话框
> 截图位置：`./screenshots/09-export.png`

---

## 四、其他说明

- 顶部操作栏支持 **「打开其他文件」** 与 **「关闭」**，方便切换或退出。
- 当前为桌面（Tauri）与浏览器双端兼容：桌面端打印走系统原生打印，浏览器端走网页打印。
- 所有裁剪计算均为本地完成，不涉及网络传输。

---

*文档生成时间：2026-09-16 · 对应实现：`src/views/CropTool.vue`*